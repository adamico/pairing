#!/usr/bin/env ruby
$LOAD_PATH.push File.join(File.dirname(__FILE__),".." ,"lib" )
require 'pairing'

cas = [2002, 38, 13]
temoin = [2002, 38, 13]

match = Pairing::Match.new(STDOUT)
match.cas(cas)
match.temoin(temoin)
match.find
